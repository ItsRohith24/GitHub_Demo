# GitHub_Demo
this is my first git repository
- ls to know all files. 
- cd <folder name> to get into the folder
- ls -Force to know hidden files
- git status to know the status
- git clone <repository-url> the repository already exists on GitHub and you want it on your computer

should commit everytime after modifying a file, after adding we have two step:

1. add
2. commit
3. push - to add it to the github site

# 4 types of files:
1. untrack
2. modified
3. staged
4. unmodified

# Add and Commit:
*Add:* git add <-file name->     *git add . to add all files*

*Commit* git commit -m "some mesage"

// to add the folders in js create new folder in GitHub demo, then add all the folder into it. then add andcommit the the folder itself so that all the files in it will be added to the github